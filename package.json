{
  "name": "jbe-school-admin-guide-web",
  "private": true,
  "description": "전북특별자치도교육청 「학교행정업무 길라잡이」 웹판",
  "scripts": {
    "build:source": "python3 scripts/reextract_source_pages.py && python3 scripts/build_structured_chapters_v2.py && node scripts/build_workflow_layout.js && node scripts/build_structured_search_index.js",
    "check": "node scripts/run_all_validations.mjs",
    "serve": "python3 -m http.server 8899 --directory docs"
  },
  "devDependencies": {
    "kordoc": "^4.4.0",
    "pdfjs-dist": "^4.10.38"
  },
  "comment": "kordoc은 원문 대조 검증에만 씁니다(scripts/validate_source_against_kordoc.mjs). OCR용 무거운 선택 의존성은 필요 없으니 'npm install --omit=optional'로 설치하세요. pdfjs-dist는 kordoc이 4.x를 쓰므로 5.x로 올리지 마세요."
}

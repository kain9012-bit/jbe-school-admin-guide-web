{
  "name": "jbe-school-admin-guide-web",
  "private": true,
  "description": "전북특별자치도교육청 「학교행정업무 길라잡이」 웹판",
  "scripts": {
    "build:source": "node scripts/build_chapters_from_hwpx.mjs && node scripts/build_workflow_layout.js && node scripts/build_structured_search_index.js",
    "check": "node scripts/run_all_validations.mjs",
    "serve": "python3 -m http.server 8899 --directory docs"
  },
  "devDependencies": {
    "kordoc": "^4.4.0",
    "pdfjs-dist": "^4.10.38"
  },
  "comment": [
    "본문과 표는 매뉴얼 한글파일(source/manual-hwpx)에서 만들어 냅니다.",
    "  npm run build:source",
    "PDF는 원문 보기 링크와 서식 자산에만 씁니다.",
    "kordoc은 한글파일을 읽는 데 씁니다. OCR용 무거운 선택 의존성은 필요 없습니다.",
    "  npm install --omit=optional      (윈도우 PowerShell에서는 npm.cmd)",
    "pdfjs-dist는 kordoc이 4.x를 쓰므로 5.x로 올리지 마세요."
  ]
}

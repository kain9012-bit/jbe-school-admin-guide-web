# 이 저장소에서 일하는 방침

전북특별자치도교육청 「학교행정업무 길라잡이」 웹판입니다.
매뉴얼 한글파일(`source/manual-hwpx`)에서 화면(`docs`)을 만들어 냅니다.

이 문서는 **지금까지 지적받은 것을 다시 하지 않기 위한 방침**입니다.
새로 작업할 때 여기부터 읽습니다.

---

## 0. 대원칙

> **원문이 답이다. 화면은 원문을 옮기는 곳이지 다시 그리는 곳이 아니다.**

원문이 표로 그린 것은 표로, 사진으로 보여 준 것은 사진으로, 병합한 칸은
병합한 채로 옮깁니다. 화면이 더 나아 보이는 모양으로 바꾸지 않습니다.

이 원칙을 어겼던 가장 큰 사례:

- 매뉴얼이 표로 그린 절차도를 카드와 화살표로 **다시 그렸습니다.** 원문의
  자리를 버리고 한 줄짜리 차례로 펴야 했는데, 가지가 갈라지는 그림은 펼 수가
  없어 카드 한 장에 열세 줄이 들어갔습니다(제8편 '촉탁직 노동자 (재)고용').

  다만 **원문이 정말 한 줄기 절차인 자리**는 이어서 그립니다. 상자와 화살표가
  번갈아 선 한 줄짜리 표가 그렇습니다. 상자도 화살표도 차례도 원문 그대로
  두고, 화면 폭에 맞춰 줄만 바꿉니다. 지어내는 것이 아니라 원문이 이어 둔
  것을 이어서 보이는 것입니다(제1편 신원조사 'e하나로민원 권한신청 및 이용').
- 원문의 사진을 **아예 꺼내지 않고** 자리 표시만 지웠습니다. 사진 밑에 달린
  이름만 남아 뜻 없는 글자 줄이 됐습니다(제1편 '문서 편철 및 보관 방법').
- 칸 안에 그려진 표를 **표가 아니라 글줄로** 실었습니다. 한글파일을 읽는 쪽이
  그 표를 한 줄로 펴 주는 대로 받아 적었기 때문입니다. 서가 그림이
  '영구 / 준영구 / 10년 / 5년 / 3년'이라는 글줄이 됐습니다
  (제1편 기록물 관리 TIP '서가배치').
- 그 서가 그림을 표로 되살리면서 이번에는 **원문 자리를 손봤습니다.** 빈 열을
  걷어내고 낱말이 안 끊기게 폭을 늘렸더니, 원문에서 폭 162mm에 든 그림이
  1300px로 부풀고 ○이 제 서가를 벗어났습니다. 그림은 손대는 순간 무너집니다.
- 절차를 잇는 **화살표를 다 버렸습니다.** 화살표는 글자가 아니라 도형이라
  그 칸이 빈 칸으로 보였고, 빈 열을 걷어내는 규칙에 함께 쓸려 나갔습니다.
  이어지던 절차가 낱개 상자로 흩어졌습니다
  (제1편 신원조사 'e하나로민원 권한신청 및 이용').
- 절차도를 **글자 덩어리로** 실었습니다. 원문이 표가 아니라 네모 도형으로
  그려 둔 자리인데, 표만 찾고 도형은 안 봤습니다
  (제1편 '5. 비전자기록물 이관 및 폐기 절차').
- 그러고도 **한글파일에 있는 것을 안 읽었습니다.** 서가 틀은 칸마다 적힌
  테두리로 그려져 있습니다(굵은 선 1.0mm = 기둥, 얇은 선 0.12mm = 선반,
  없음 = 트인 쪽). 그것을 안 읽고 모든 칸에 똑같은 선을 그으니 서가가
  모눈종이가 됐습니다. 줄 높이도 마찬가지여서 빈 대장 서식이 납작해졌습니다.
- 글머리표의 **위계를 눈대중으로 정했습니다.** '▪ ▸ • □ ‣'를 한 묶음으로
  보아 모두 0단계에 놓았더니, 거느린 '▪'가 딸린 '▸'보다 오히려 오른쪽에
  섰습니다. 원문은 '▪' 아래에 '▸'를 두 칸 들여 적습니다(217곳, 반대는 0곳).
  들여쓰기의 답도 한글파일에 이미 적혀 있었습니다
  (제3편 신분 및 권익보장 TIP '파면과 해임의 차이점').
- 표의 **열 너비를 원문과 다르게 다시 정했습니다.** 한글파일에 칸마다 너비가
  적혀 있는데(hp:cellSz), '낱말이 끊기지 않게' 열을 넓히다가 그 비율을 통째로
  갈아엎었습니다. 내용이 든 열이 80%에서 55%로 눌리고, 근거 이름만 든 열이
  13%에서 42%로 부풀었습니다. 원문 너비가 있는 표 280개 가운데 167개가
  그랬습니다(제7편 보수작업 '3. 급여 및 수당의 이해').

  한글을 '끊으면 안 되는 낱말'로 본 것이 화근이었습니다. 한글은 글자마다
  줄을 바꿀 수 있고 **매뉴얼도 그렇게 적혀 있습니다** —
  '대우공무원수당(지방공무원수당등에관한규정제5조의2)'은 원문 79px 칸에서
  다섯 줄로 접힙니다(hp:lineseg 5개). 접히는 모습까지 원문에 적혀 있었습니다.

- 칸 안의 **문단 나눔을 통째로 버렸습니다.** 한글파일은 문단마다 따로
  적어 두는데(hp:p), 글자를 읽어 오는 쪽이 한 줄로 이어 붙인 것을 그대로
  받았습니다. 공가 사유 열한 가지가 마침표도 없이 한 문단이 되어, 어디서
  하나가 끝나고 다음이 시작하는지 알 수 없었습니다
  (제4편 휴가 '2. 공가 사유').

  화면 쪽도 '글머리표 없이 시작하는 줄은 앞줄에 이어지는 줄'로 어림잡아
  붙였습니다. 매뉴얼 본문은 글머리표 없는 문단이 훨씬 많습니다. 앞줄에
  이어지는 줄인지 아닌지도 원문이 이미 말해 줍니다 — 그런 줄만 한두 칸
  들여 씌어 있습니다.

바꾸고 싶은 모양이 있으면 먼저 원문을 열어 봅니다. 원문에 그렇게 되어 있지
않으면 바꾸지 않습니다.

---

## 1. 일하는 절차

지적 한 건을 고칠 때 그 한 건만 고치지 않습니다. **같은 원인을 가진 것을
전부 찾아 고칩니다.** 지적은 표본이지 목록이 아닙니다.

1. **재현한다** — 짐작하지 않고 데이터를 찍어 봅니다.
2. **전수 조사한다** — 같은 원인이 몇 군데인지 셉니다. 한 건만 고치고
   넘어가면 같은 지적을 또 받습니다. 실제로 그랬습니다.
   - 셀병합: 한 표만 고쳤다가 다시 지적받았습니다. 세어 보니 38개였습니다.
   - 서식 이름: 제7편만 보다가, 세어 보니 20개가 문장 조각이었습니다.
   - 절차 그리기: 캡처로 받은 '한 줄짜리 표'만 보고 규칙을 짰습니다.
     세어 보니 서른다섯 개였고, 그 가운데 다섯 개만 고쳐졌습니다.
     **"캡처해 준 것만 바꾼다"는 말을 들었습니다.**
   → 규칙을 짜기 전에 그 규칙이 몇 개를 잡는지부터 셉니다. 캡처 한 장은
   표본입니다. 표본에 맞춘 규칙은 표본만 고칩니다.
3. **원인 층에서 고친다** — 화면에서 가리지 않고 만들어 내는 곳에서 고칩니다.
4. **검사기를 만든다** — 고치기 전 코드에서 **반드시 실패하는지 확인합니다.**
   실패하지 않는 검사기는 아무것도 지키지 못합니다.
5. **전체 검사를 돌린다** — `npm run check`와 `npm run validate:browser`.
6. **눈으로 본다** — 검사기가 다 통과해도 화면을 열어 그 자리를 실제로
   봅니다. 검사기는 '무너지지 않았는지'를 보지 '보기 좋은지'를 못 봅니다.
   서가 배치도를 표로 되살리고 검사기를 다 통과시킨 뒤 "다 됐습니다"라고
   했는데, 화면에는 스물일곱 열짜리 격자가 가로 스크롤을 달고 ○이 제 서가를
   벗어나 있었습니다. 열어 봤으면 한눈에 보였습니다.
7. **재는 수치를 남긴다** — "좋아졌다"가 아니라 "38개 → 1개"로 적습니다.

### 고칠 수 없는 것은 고쳤다고 하지 않는다

원문 자체가 이상한 곳이 있습니다(점을 빠뜨린 소제목, 그림으로 그린 화살표).
그런 것은 그렇다고 적어 두고 넘어갑니다. 지어내서 채우지 않습니다.

---

## 2. 지적받은 것과 그것을 지키는 검사기

| 지적 | 방침 | 지키는 검사기 |
| --- | --- | --- |
| 서식 미리보기 글자·표를 복사할 수 없다 | 미리보기는 그림 한 장이 아니라 진짜 글자로 편다. 한글 붙여넣기는 CF_HTML이라야 표가 살아난다 | `validate_form_text_copy.mjs` |
| 머리글이 스크롤하면 사라진다 | `position: sticky`가 서는 조상을 깨지 않는다 | `validate_navigation.mjs` |
| 소제목 서식이 들쭉날쭉하다 | 본문 줄에 소제목이 숨지 않는다 | `validate_block_presentation.js` |
| 업무 흐름도가 도막났다 | 원문에서 화살표로 이어진 자리는 화면에서도 한 줄이다 | `validate_source_presentation.mjs` |
| 글머리표가 어떤 건 있고 어떤 건 없다 | 원문 글머리표를 그대로 살린다 | `validate_bullet_lines.js` |
| 같은 기호인데 들여쓰기가 다르다 | 기호마다 단계를 고정한다(`MARKER_LEVEL`). 항목마다 다시 매기지 않는다 | `validate_source_presentation.mjs` |
| 딸린 항목이 거느린 항목보다 바깥에 선다 | 기호의 단계는 **한글파일의 앞공백을 세어** 정한다. 보기 좋아 보이는 순서로 정하지 않는다 | `validate_marker_levels.py`, `validate_source_presentation.mjs` |
| 여러 문단이 한 줄로 뭉쳐 있다 | 칸 안의 줄은 원문 문단(`hp:p`) 하나하나다. 앞줄에 이어지는 줄은 원문에서 들여 쓴 줄뿐이다 | `validate_cell_lines.py`, `validate_cell_line_breaks.mjs` |
| 항목이 여러 줄로 넘어가 어디서 끝나는지 안 보인다 | 넘어가는 항목이 있는 칸은 항목마다 앞에 기호를 세운다. 넘어가는지는 **그려 놓은 것을 재서** 정한다(글자 수로 어림잡으면 좁은 칸을 놓친다) | `validate_cell_line_breaks.mjs` |
| 그림형 표만 흰 상자 없이 떠 있다 | 원문에 없는 것은 그림을 두른 '선'이지 그림을 받치는 '종이'가 아니다. 본문에 홀로 놓인 표는 종류를 가리지 않고 같은 바탕 위에 선다 | `validate_table_fit.mjs` |
| 내용이 든 열이 눌리고 이름만 든 열이 부풀었다 | **원문 열 너비가 바탕**이다. 최소 몫에 못 미치는 열만 끌어올린다. 한글은 글자마다 접히므로 '끊으면 안 되는 낱말'이 아니다(원문도 그렇게 접혀 있다) | `validate_table_widths.mjs` |
| 표 상자에 세로 스크롤이 생겨 아래가 잘린다 | 키는 **늘리기만 하고 못박지 않는다**(`min-height`). 가로 스크롤 상자에 키를 못박으면 넘치는 내용이 잘리고, 못박힌 키는 줄 높이를 정할 때 제 내용을 세지도 않는다 | `validate_table_fit.mjs` |
| 절차 단계 안에 가로 스크롤이 생긴다 | 폭은 원문 비율로 나누되, **제 내용이 안 들어가는 단계만 넓히고** 그만큼 여유 있는 단계에서 덜어 온다. 단계 안에 든 표가 드는 폭까지 함께 본다 | `validate_table_fit.mjs` |
| 절차도에 속이 빈 상자가 줄줄이 생긴다 | **그림으로 그린 표는 절차 카드로 다시 그리지 않는다.** 그림에서는 빈 칸이 곧 모양(갈래·화살표 자리)이라, 단계로 자르면 빈 칸마다 테두리가 둘린다 | `validate_table_shape.js` |
| 상자 하나에 단계 둘이 갇히고 그 사이에 빈 띠가 남는다 · ⇩가 상자 안 한 줄로 들어앉는다 | 원문은 긴 절차를 **접어서** 아래 단으로 내려 쓴다. 접힌 자리는 **표 폭을 통째로 덮는 트인 띠**(테두리 왼·오른이 NONE)에 읽을 글이 없는 줄이고, 거기 놓인 ⇩가 다음 단을 가리킨다. 단마다 따로 그리고 그 사이에 원문의 ⇩를 세운다(원문이 오른쪽에서 접었으면 오른쪽에). **가로로 늘어선 절차만이 아니라 세로로 내려가는 절차도 같다.** 화살표가 몇 개든 **제 열 자리에** 세운다(나란한 절차 둘이 함께 접히기도, 한 줄기가 둘로 갈리기도 한다). 폭이 모자란 띠는 세로로 병합된 이름칸이 지나가는 자리라 표를 자를 수 없다 — 그 자리는 **선만 지운다** | `validate_flow_folds.py` · `validate_flow_lanes.mjs` |
| 한 단계 상자 안에 단계 둘이 갇히고 그 사이에 빈 줄이 남는다 | 한 열에서만 벌어진 자리다. 원문 테두리가 그렇게 말한다 — **양옆에는 선이 없고 위아래에만 선이 있다**(`nnss`). 위 상자의 아랫선과 아래 상자의 윗선이다. 그 자리에서 상자를 둘로 나눠 쌓는다(제12편 '4. 물품취득 후 출급절차') | `validate_flow_lanes.mjs` |
| 접힌 표의 첫 상자만 글씨가 굵다 | 단마다 줄 수가 같으면 머리글 줄이 없다. 같은 모양이 되풀이될 뿐인데 첫 단의 첫 줄만 머리글로 잡으면 그 상자만 달라 보인다 | `validate_flow_lanes.mjs` |
| 본문에 편 이름·표지 글과 띠 그림이 끼어 있다 | 쪽 머리글·꼬리글은 지면 장식이지 본문이 아니다. **무엇이 머리글인지는 한글파일(`hp:header`·`hp:footer`)이 말해 준다.** 문단째로 걷어 낸다(글자만 지우면 띠 그림이 남는다) | `validate_running_heads.py` |
| 본문 끝에 빨간 띠 그림만 덩그러니 남아 있다 | 글자와 그림은 **서로 다른 덩이**로 온다. 문단에 세운 문지기는 그림 덩이를 못 본다. 지면 장식 그림은 **크기로 어림잡지 말고** 쪽 바탕(`Contents/masterpage*.xml`)·머리글에서만 부르는 이름인지로 가린다. 본문이 한 번도 안 부르는 그림은 꺼내지도 않는다 | `validate_running_heads.py` |
| 표에 가로 스크롤이 생긴다 | 원문 열 너비를 쓰되 폭 안에 맞춘다 | `validate_table_fit.mjs` |
| 표 세로선이 끊긴다 | 병합된 표에서 `:first-child`로 테두리를 지우지 않는다 | `validate_table_fit.mjs` |
| ▤ 깨진 글자가 보인다 | 함초롬 글꼴 개인용 영역(PUA) 글자를 보통 기호로 바꾼다 | `validate_no_broken_glyphs.js` |
| TIP이 맨 아래 몰려 있다 | TIP은 원문에 놓인 자리에 그대로 둔다 | `validate_tip_placement.mjs` |
| 서식 이름이 문장 조각이다 | 이름은 서식 파일에 적힌 제목에서 딴다. 본문에서 딸 때는 짝 없는 괄호에서 자른다 | `validate_form_titles.js` |
| 서식이 엉뚱한 업무에 붙어 있다 | 본문이 부른 자리가 답. 부르지 않으면 이름을 함께 보고 짐작한다 | `validate_form_placement.mjs` |
| 셀병합이 풀리고 왼쪽 열이 넓다 | 한글파일 칸 주소(`hp:cellSpan`)와 열 너비(`hp:cellSz`)를 그대로 쓴다 | `validate_table_merges.mjs`, `validate_hwpx_cell_text.py` |
| 화살표 자리로 비워 둔 열이 남는다 | 글이 든 칸은 '시작하는' 열·행만 채운다. 걸친 칸이 지나간다고 살리지 않는다 | `validate_table_shape.js` |
| 원문 표를 맘대로 카드로 그린다 | 표는 원문 자리에 표로 그린다 | `validate_table_fit.mjs` |
| 절차 단계 상자의 키가 제각각이다 | 한 줄에 나란히 선 단계는 키를 맞춘다. 눈에 보이는 상자를 재야 한다(칸은 이미 늘어나 있어 늘 같습니다) | `validate_card_heights.mjs` |
| 절차인데 그냥 표로 그려 흐름이 안 보인다 | **세로줄이 통째로 화살표인 자리**가 단계의 경계다. 줄 수는 상관없다. 상자·화살표·차례는 원문 그대로, 줄만 화면 폭에 맞춘다 | `validate_table_shape.js`, `validate_table_fit.mjs` |
| 표 칸의 사진이 통째로 빠졌다 | 사진이 곧 그 칸의 내용이다. 한글파일을 읽는 쪽이 **글자만** 읽으면 그 칸은 빈 칸이 된다 — 그림도 글자와 같은 자리에 담아 온다(`hp:pic` → `[[그림:imageNN]]`). 꺼낼지 말지는 **크기가 아니라 자리**로 정한다(표 칸에 놓였으면 본문 그림이다). 도형 안의 그림은 뺀다 — 그 자리는 도형 쪽에서 되살린다 | `validate_cell_pictures.py` |
| 원문 사진이 다 없어졌다 | 한글파일 `BinData`에서 그림을 꺼내 본문 자리에 그린다 | `validate_manual_pictures.mjs` |
| 사진만 넣고 이름은 한 줄에 몰아 붙였다 | 원문은 안쪽 표라 사진 아래 칸이 그 사진의 이름이다. 사진마다 제 이름을 단다 | `validate_manual_pictures.mjs` |
| 사진 넉 장이 3+1로 접혔다 | 원문이 한 줄짜리 표면 화면도 한 줄이다. 칸 수만큼 폭을 나눈다(크기로 맞추지 않는다) | `validate_manual_pictures.mjs` |
| 칸 안에 든 표를 통째로 빼먹었다 | 칸 안의 표도 표다. 한글파일에 적힌 안쪽 표의 칸 주소로 되살린다 | `validate_nested_tables.mjs` |
| 표로 그린 그림을 보통 표로 그려 무너뜨렸다 | 빈 칸이 곧 모양인 표는 원문 자리와 비율을 그대로 쓴다. 빈 열을 걷지도, 낱말 폭에 맞춰 늘리지도 않는다 | `validate_table_shape.js` |
| 서가 그림이 모눈종이가 됐다 | 칸마다 어느 쪽에 선을 긋는지가 한글파일에 적혀 있다. 그대로 긋는다. 줄 높이도 그대로 쓴다 | `validate_table_shape.js` |
| 도형으로 그린 절차도가 글자 덩어리가 됐다 | 네모(`hp:rect`)마다 자리와 크기가 적혀 있다. 같은 줄에 선 것끼리 묶어 표로 되살린다 | `validate_shape_tables.py` |
| 절차를 잇는 화살표가 다 없어졌다 | 화살표는 도형(`hp:polygon`)이다. 꼭짓점을 보고 방향을 읽어 그 칸에 세운다. 글자가 없다고 그 열을 걷지 않는다 | `validate_shape_tables.py`, `validate_table_shape.js` |
| 상자 안에 든 세부내용이 앞 항목에 딸려 붙었다 | 상자 속에 세부내용 표가 있으면 그것이 항목 이름이다. 새 항목으로 연다 | `validate_shape_tables.py` |
| 검색에 새 내용이 안 잡힌다 | 색인은 `build:source`에서 늘 다시 만든다 | `validate_search_quality.js` |

검사기는 40개가 넘습니다. `scripts/validate_*` 파일마다 머리에 **무엇을 왜
보는지** 적혀 있습니다. 새 검사기도 그렇게 적습니다.

### 되풀이해서 나온 원인 네 가지

같은 실수가 다른 얼굴로 여러 번 나왔습니다. 새 코드를 쓸 때 먼저 의심합니다.

1. **글이 많은 쪽이 이기는 점수** — 겹치는 낱말 '개수'로 고르면 큰 문서가
   늘 이깁니다. 서식이 죄다 제7편 보수작업에 붙었고, 검색에서도 '육아휴직
   신청 방법'에 제7편이 1위였습니다.
   → 낱말마다 값을 다르게 줍니다(여러 곳에 나오는 낱말은 값을 낮춤).
   쌓아 올리는 점수에는 뚜껑을 씌웁니다.

2. **원문에 있는 것을 아예 안 보기** — 화면에 옮기는 쪽이 못 읽는 것은
   없는 것이 되어 버립니다. kordoc은 칸 안에 든 표를 행마다 한 줄, 칸 사이는
   빗금으로 이어 붙여 바깥 칸의 글로 폅니다. 원문에서 표였던 244자리가
   화면에서는 뜻 없는 글줄 더미였습니다.
   → 옮기는 쪽이 무엇을 버리는지 먼저 확인합니다. 한글파일에는 안쪽 표도
   제 칸 주소를 그대로 가지고 있고, 어느 칸에 들었는지도 적혀 있습니다.

   같은 실수를 세 겹으로 했습니다. 안쪽 표 → 원문 자리 → 칸 테두리.
   **화면에 안 나오는 것이 있으면 한글파일 XML을 먼저 열어 봅니다.**
   한글파일에 이미 들어 있는 것(아직 안 읽고 있는 것):

   | 있는 것 | 어디에 | 쓰는 곳 |
   | --- | --- | --- |
   | 칸 주소·병합 | `hp:cellAddr` `hp:cellSpan` | 표 모양 |
   | 열 너비·줄 높이 | `hp:cellSz` | 원문 비율 |
   | 칸 테두리 | `hp:tc@borderFillIDRef` → `header.xml` | 그림의 선, 상자와 상자를 가르는 트인 띠 |
   | 안쪽 표 | `hp:tc` 안의 `hp:tbl` | 칸 안의 표 |
   | 도형으로 그린 표 | `hp:container` 안의 `hp:rect` + `hp:offset` | 절차도 |
   | 화살표 | `hp:polygon`의 꼭짓점(`hp:pt`) | 절차를 잇는 표시 |
   | 그림 | `BinData` | 본문 사진 |
   | 줄 앞 들여쓰기 | 글자 앞의 공백·`hh:paraPr`의 여백 | 글머리표의 위계, 이어지는 줄 |
   | 칸 안의 문단 나눔 | `hp:tc` 안의 `hp:p` 하나하나 | 칸 안의 줄 나눔 |
   | 줄이 접히는 자리 | `hp:p` 안의 `hp:linesegarray` | 원문에서 어디서 접히는지 |
   | 쪽 머리글·꼬리글 | `hp:header` · `hp:footer` | 본문이 아닌 지면 장식 가리기 |
   | 쪽 바탕 | `Contents/masterpage*.xml` | 머리 띠·쪽 바탕 그림 가리기 |
   | 그림을 부르는 이름 | `binaryItemIDRef` | 그 그림이 본문 것인지 장식인지 |
   | 아직 안 읽는 것 | 칸 배경색, 글자 꾸밈, 대각선(`hh:slash`), 잇는 선(`hp:line`) | — |

   **표가 아닌 것이 표일 수 있습니다.** 매뉴얼은 절차도를 네모 도형으로 그려
   두기도 합니다. `hp:tbl`만 찾으면 그 자리는 글자 덩어리로 남습니다.

   **글자가 없는 칸이라고 빈 칸이 아닙니다.** 화살표는 도형입니다. 글자만 보고
   '빈 칸'이라 판단해 그 열을 걷어내면 절차가 흩어집니다.

3. **넣기만 하고 원문 모양은 안 보기** — 빠진 것을 채워 넣고 끝내면, 원문이
   그것을 어떻게 놓았는지는 여전히 틀린 채입니다. 같은 사진 하나로 세 번
   지적받았습니다.
   - 1차: 사진이 아예 없었다 → 꺼내 실었다
   - 2차: 이름이 한 줄에 몰려 있다 → 원문은 안쪽 표, 사진마다 이름을 달았다
   - 3차: 넉 장이 3+1로 접혔다 → 원문은 한 줄짜리 표, 칸 수만큼 폭을 나눴다
   → 넣은 뒤에 **원문을 다시 열어 자리·짝·줄까지 맞춰 봅니다.**
   화면이 알아서 접게 두지 말고, 원문의 칸 수를 그대로 씁니다.

4. **한쪽에만 있는 글자로 짝을 맞추기** — 화살표(⇒ ➡ ×), 자동 글머리표,
   쪽 머리글, 안쪽 표를 펼 때 붙는 빗금. 이 한 글자 때문에 표 38개가 짝을
   잃고 병합을 통째로 날렸습니다.
   → 짝을 맞출 때는 꾸밈 글자를 다 빼고 글자만 봅니다(`bare`).

   같은 뿌리에서 나온 것이 계속 나옵니다. 새로 찾은 것 넷:
   - **한쪽에만 남는 기호** ▪ (사진만 든 칸은 한글파일에 ▪가, kordoc에는
     아무것도 없습니다) → `bare`에서 뺍니다.
   - **한쪽에서만 버려지는 글자** ○ (kordoc 쪽에서 '글자가 없는 조각'이라며
     스무 개를 버렸습니다) → 버리는 것은 글머리표뿐입니다.
   - **차례가 다른 글자** 표 옆에 붙인 '(단위: 천원)'을 한글파일은 표 뒤에,
     kordoc은 표 앞에 둡니다 → 글자를 늘어놓아 견줍니다.
   - **한쪽에만 있는 자동 번호** '1.' → kordoc에서 몇 글자만 빼면 한글파일
     글이 되는지 봅니다(넉 자까지).

---

## 3. 만드는 차례

```bash
npm run build:source    # 한글파일 → 본문·표·그림·흐름·색인 (원문이 바뀌면)
npm run build:extras    # 서식 미리보기·FAQ (10분쯤)
npm run check           # 데이터 검사 (빠름)
npm run validate:browser # 브라우저로 보는 검사 (오래 걸림)
```

`build:source`는 이 차례를 지켜야 합니다. 앞이 뒤의 재료입니다.

```
read_hwpx_tables.py      한글파일 칸 주소(병합·열너비)
extract_manual_images.py 본문 사진
build_chapters_from_hwpx.mjs  본문·표
build_workflow_layout.js      단계 배치
build_structured_search_index.js  검색 색인
stamp_asset_versions.js       자산 번호
```

색인과 배치를 다시 만들지 않으면 새 내용이 검색에 안 잡히고, 새 블록이
화면에서 사라집니다. 실제로 그랬습니다.

---

## 4. 운영 규칙

- **샌드박스에서 `git`을 실행하지 않습니다.** `git status`처럼 읽기만 하는
  명령도 `.git/index.lock`을 만드는데, 마운트 권한 때문에 지우지 못합니다.
  남은 lock 때문에 사용자 쪽에서 커밋이 통째로 막힙니다.
  git은 사용자에게 PowerShell 명령으로 넘깁니다.
- 이 저장소는 GitHub Actions가 날마다 자동 커밋·푸시하므로 `git push` 전
  `git pull --rebase`가 거의 늘 필요합니다.
- 주석은 한국어로, **무엇을 하는지가 아니라 왜 그렇게 했는지**를 적습니다.
  고치기 전에 무엇이 잘못됐는지를 예와 함께 남깁니다.
- 작업 사본에서만 나는 실패 3건은 원래 그렇습니다(내려받기 원본 파일,
  벤더 아이콘이 저장소에 없음, 서식 미리보기 글자 겹침 2건).
